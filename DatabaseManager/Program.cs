namespace DatabaseManager
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Usage
            
            using (var dbContext = new CandleStickDbContext())
            {
                var data = dbContext.CandleStickData.ToList();
            }
            */
        }
    }
}
